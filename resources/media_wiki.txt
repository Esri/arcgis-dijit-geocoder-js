__NOTOC__
__NOEDITSECTION__

{{#css:/custom/jsapi.css}}

== class esri.dijit.Autocomplete ==
<div style="font-family: consolas, monospace;">
dojo.require("esri.dijit.Autocomplete");<br/>
defined in esri/dijit/Autocomplete.js
</div>

Autocomplete widget provides a ready-to-use widget to locate results from a locator.
This dijit lets users ...

=== Class hierarchy ===
<pre style="background-color: white; border: none;">
             +------------------------------+
             ¦            Object            ¦
             +------------------------------+
                  &Delta;                   &Delta;
                  |                   |
                  |                   |
         +----------------+  +--------------------+
         ¦  dijit._Widget ¦  ¦  dijit._Templated  ¦
         +----------------+  +--------------------+
                  &Delta;                   &Delta;
                  |                   |
                  |                   |
             +------------------------------+
             ¦  esri.dijit.Autocomplete  ¦
             +------------------------------+
</pre>

=== Constructor ===

{| class="gmaps"
|-
! new esri.dijit.Autocomplete ( params, srcNodeRef )
|-
| Creates a new Autocomplete dijit using the given DOM node.<br/>
<b>Parameters:</b><br/>
{| class="gmaps" style="width:auto;"
|-
! Name
! Type
! Description
|-
| class="code" | params
| class="code" | Object
| Various parameters to configure this dijit. See the list below for details.
|-
| class="code" | srcNodeRef
| class="code" | DOMNode | String
| Reference or id of a HTML element that this dijit is rendered into. This parameter is always required.
|}
<br/>
The following parameters are supported:
{| class="gmaps" style="width:auto;"
|-
! Name
! Type
! Description
|-
| class="code" | map
| class="code" | [http://resources.esri.com/help/9.3/arcgisserver/apis/javascript/arcgis/help/jsapi_start.htm#jsapi/map.htm Map]
| Map object that this dijit is associated with. This parameter is always required.
|-
| class="code" | locators
| class="code" | Array
| Required. Defines the elements of the layout.
  [
    {
        url: string,
        name: string,
    },
    {
        url: string,
        name: string,
    }
  ]
|
|-
| class="code" | theme
| class="code" | String
| Optional. Adds the theme class to the container node of the widget for styling. Default: 'esriAutocomplete'.
|-
| class="code" | placeholder
| class="code" | String
| Optional. Placeholder text shown in the search input field. Default: 'Find address or place'.
|-
| class="code" | resetTitle
| class="code" | String
| Optional. Title attribute for the button that clears the input field. Default: 'Clear Location'.
|-
| class="code" | zoomLevel
| class="code" | Number
| Optional. Level of detail to zoom the map into if the locator doesn't return an extent. Default: 12.
|-
| class="code" | maxLocations
| class="code" | Number
| Optional. Maxiumum number of results to return in the autocomplete. Default: 6.
|-
| class="code" | minCharacters
| class="code" | Number
| Optional. Minimum amount of characters required to be entered into the search field before querying for autocomplete results. Default: 3.
|-
| class="code" | hideDelay
| class="code" | Number
| Optional. Number of milliseconds before automaticly hiding the autocomplete results. Default: 6000.
|-
| class="code" | searchDelay
| class="code" | Number
| Optional. Number of milliseconds before querying for autocomplete results. Default: 300.
|-
| class="code" | useBoundingBox
| class="code" | Boolean
| Optional. Only searches for results that are withihn the current map's extent. Default: false.
|}

|}


=== Properties ===

{| class="gmaps" style="width:auto;"
|-
! Property
! Type
! Description

|-
| class="code" | value
| class="code" | String
| Current value of the Autocomplete input textbox
|-
| class="code" | activeLocator
| class="code" | Number
| Current locator index to search by default. Defaults to 0 on init.
|}


=== Methods ===

{| class="gmaps"
|-
! Methods
! Return Value
! Description
|-
| class="code" | startup()
| class="code" | none
| Finalizes the creation of this dijit. This method should be called after the constructor for this dijit is called and before letting the users interact with it.
|-
| class="code" | destroy()
| class="code" | none
| Releases all resources used by the dijit. This method should be called when an instance of this dijit is no longer needed in your application.
|-
| class="code" | getResults()
| class="code" | none
| Returns the last results from an autocomplete or search.
|-
| class="code" | clear()
| class="code" | none
| Clears any value currently set in the autocomplete.
|-
| class="code" | hide()
| class="code" | none
| Hides any autocomplete results that may be showing.
|}

=== Events===

{| class="gmaps"
|-
! Event
! Return Value
! Description
|-
| class="code" | onLocate
| class="code" | String
| Event is fired when a result has been selected, the submit button is pressed or enter key is fired.
|-
| class="code" | onAutoComplete
| class="code" | String
| Event is fired when autocomplete has started.
|-
| class="code" | onAutoCompleteResults
| class="code" | String
| Event is fired when there results are returned from an autocomplete.
|}


=== CSS Classes ===

The following table lists the CSS class names defined by the basemap gallery to refer to the DOM nodes that it creates. These class names can be used to customize the look and feel of this widget in your application.

{| class="gmaps"
|-
! Class Name
! Description
|-
| class="code" | esriAc
| Represents a node where an instance of the autocomplete widget is rendered.
|-
| class="code" | esriAcActive
| This class is applied to the esriAc node when the autocomplete results menu is currently active.
|-
| class="code" | esriAcNoResults
| Represents the currently selected gallery item node containing a thumbnail and a label.
|-
| class="code" | esriAcResults
| Represents a node that contains a thumbnail image.
|-
| class="code" | esriAcResultsLoading
| Represents a node that contains a thumbnail image.
|-
| class="code" | esriAcItem
| Represents a node that contains a thumbnail image.
|-
| class="code" | even
| Represents a node that contains a thumbnail image.
|-
| class="code" | odd
| Represents a node that contains a thumbnail image.
|-
| class="code" | esriAcPartial
| Represents a node that contains a thumbnail image.
|-
| class="code" | esriAcSearch
| Represents a node that contains a thumbnail image.
|-
| class="code" | esriAcClear
| Represents a node that contains a thumbnail image.
|-
| class="code" | esriAcClearActive
| Represents a node that contains a thumbnail image.
|-
| class="code" | esriAcHeader
| Represents a node that contains a thumbnail image.
|-
| class="code" | esriAcClearFloat
| Represents a node that contains a label.
|}

=== Usage ===

==== Create the dijit with default parameters ====
<pre style="border: none;">
 var autocomplete = new esri.dijit.Autocomplete({ map: map }, "autoCompleteDiv");
 autocomplete.startup();

 HTML:

 <div id="map"></div>
 <div id="autoCompleteDiv" style="position: absolute; z-index: 2; top: 20px; left: 100px;"></div>
</pre>

When the dijit is created in this fashion none of the Bing maps will show up in the basemap gallery.

==== Create the dijit with custom parameters ====
<pre style="border: none;">

    var autocomplete = new esri.dijit.Autocomplete({
        theme: 'esriAutocomplete', // Used to add a class to style the autocomplete
        placeholder: 'Find address or place',
        resetTitle: 'Clear Location',
        zoomLevel: 6,
        locators:[
            {
                url:'http://geocode.arcgis.com/arcgis/rest/services/World/GeocodeServer',
                name:'World'
            },
            {
                url: 'http://dexter2k8/gis/rest/services/IHS_Composite/GeocodeServer',
                name: 'IHS_Composite'
            }
        ],
        maxLocations: 3,
        minCharacters: 3,
        useBoundingBox: false, // don't search within current extent
        hideDelay: 6000,
        searchDelay: 300,
        map: map
    }, dojo.byId('search'));
    autocomplete.startup();


 HTML:

 <div id="map"></div>
 <div id="search" style="position: absolute; z-index: 2; top: 20px; left: 100px;"></div>
</pre>


=== Samples ===

http://webninja.esri.com/git/Autocomplete-Widget/index.html <br/>
http://webninja.esri.com/git/Autocomplete-Widget/index2.html


